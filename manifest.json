{
  "manifest_version": "0.3",
  "name": "mcp-ecommerce-platform-profiler",
  "version": "1.0.0",
  "description": "MCP server that detect the ecommerce platform, payment providers and catalogue size behind a storefront.",
  "author": {
    "name": "Mamba Labs"
  },
  "license": "MIT",
  "homepage": "https://github.com/mambalabsdev/mcp-ecommerce-platform-profiler",
  "repository": {
    "type": "git",
    "url": "https://github.com/mambalabsdev/mcp-ecommerce-platform-profiler"
  },
  "server": {
    "type": "node",
    "entry_point": "build/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/build/index.js"
      ],
      "env": {
        "APIFY_TOKEN": "${user_config.apify_token}"
      }
    }
  },
  "user_config": {
    "apify_token": {
      "type": "string",
      "title": "Apify API Token",
      "description": "Apify API token for running the actor",
      "sensitive": true,
      "required": false
    }
  }
}
